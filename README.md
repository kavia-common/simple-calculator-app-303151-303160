# Simple Calculator App

This repository contains a single React frontend container:

- `simple_calculator_frontend/`: A responsive, Rainbow Burst themed calculator app (client-side only).

## Quick start

```bash
cd simple_calculator_frontend
npm install
npm start
```

Open http://localhost:3000
